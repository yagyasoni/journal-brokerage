import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export const metadata = {
  title: "Order a Search",
  description: "Send us the property, scope, and county to open a file.",
};

export default function OrderPage() {
  return <PagePlaceholder eyebrow="Place an order" title="Order a search." />;
}
